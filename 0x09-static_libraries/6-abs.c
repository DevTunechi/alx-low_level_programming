#include <inttypes.h>
#include "main.h"
/**
 * _abs- Absolute value
 * @num: Prints and integer
 * Return: Always return (0)
 */


int _abs(int num)

{
if (num < 0)
{
return (-num);
}
else
{
return (num);
}
}
